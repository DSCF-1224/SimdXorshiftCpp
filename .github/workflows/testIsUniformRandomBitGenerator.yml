name: TestIsUniformRandomBitGenerator
on: [push, workflow_dispatch]
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - run:  |
              echo "Test; IsUniformRandomBitGenerator"
              sudo apt update
              sudo apt install gcc-14 g++-14
              g++-14 --version
      - uses: actions/checkout@v4
